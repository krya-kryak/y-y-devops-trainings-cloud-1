name: catgpt
run-name: ${{ github.actor }} is building catgpt
on:
  push:
    branches:
      - 'main'
jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      -
        name: build bin
        uses: docker/build-push-action@v5